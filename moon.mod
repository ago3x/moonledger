name = "ago3x/moonledger"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "MIT"

keywords = [ "csv", "reconciliation", "cli" ]

preferred_target = "js"

description = "Traceable CSV reconciliation with duplicate isolation and exact decimal comparison"
